plugins {
    id("org.example.greeting") version "1.2" apply false
}

subprojects {
    apply plugin: "java"
    apply plugin: "org.example.greeting"

    dependencies {
        testImplementation("junit:junit:4.12")
    }

    repositories {
        jcenter()
    }
}
